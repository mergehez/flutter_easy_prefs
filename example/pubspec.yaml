name: easy_prefs_example
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  easy_prefs: ^0.0.2
  flutter:
    sdk: flutter
  provider: ^6.0.2
  shared_preferences: ^2.0.11

dev_dependencies:
  easy_prefs_gen: ^0.0.2
  build_runner: ^2.1.8
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
